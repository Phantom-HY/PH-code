#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int n, m;
int k;
int a[100005];
int main(){
#ifndef ONLINE_JUDGE
    freopen("data.in", "r", stdin);
    // freopen("data.out", "w", stdout);
#endif
    scanf("%d%d", &n, &m);
    while(m--)
    {
        int l,r;
        scanf("%d%d%d", &k, &l, &r);
        
    }
    return 0;
}