#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int t;
int h, g;
int main(){
#ifndef ONLINE_JUDGE
    freopen("data.in", "r", stdin);
    // freopen("data.out", "w", stdout);
#endif
    scanf("%d", &t);
    while(t--)
    {
        scanf("%d%d", &h, &g);
        
    }
    return 0;
}